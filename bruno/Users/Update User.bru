meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{base_url}}/users/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "John Updated",
    "email": "john.updated@example.com"
  }
}
