meta {
  name: Revoke Approval
  type: http
  seq: 2
}

post {
  url: {{base_url}}/versions/{{version_id}}/approvals/revoke/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
