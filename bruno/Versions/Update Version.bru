meta {
  name: Update Version
  type: http
  seq: 4
}

put {
  url: {{base_url}}/versions/{{version_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "change_summary": "Updated changes"
  }
}
