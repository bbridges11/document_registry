meta {
  name: Remove Stakeholder
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/documents/{{document_id}}/stakeholders/{{user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
