meta {
  name: Approve Deprecation
  type: http
  seq: 2
}

post {
  url: {{base_url}}/versions/{{version_id}}/deprecation/approve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "role": "deprecation-approver",
    "comment": "Approved"
  }
}
